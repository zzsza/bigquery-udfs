CREATE OR REPLACE FUNCTION hi.kyle.h3_to_geojson_boundary(h3_index STRING)
  RETURNS STRING
  LANGUAGE js AS
"""
var geojson = {
    "type": "Polygon", 
    "coordinates": [h3.h3ToGeoBoundary(h3_index,true)]
}

return JSON.stringify(geojson);
"""
OPTIONS (
  library=[gs://'your_path.js]
);
